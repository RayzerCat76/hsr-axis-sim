# Preview — Resume HSR-AXIS-002A After Tool Output Is Annotated

Do not run this next task until the Video Trace Assistant has generated an intake package and the worksheet has been manually filled.

Future task:

```text
HSR-AXIS-002A-RESUME: Convert annotated intake draft into first real manual video trace fixture.
```

Expected inputs:

- completed `trace_annotation_worksheet.md`;
- updated `draft_trace.json` with confirmed fields;
- selected frame references for each action;
- known uncertain fields marked explicitly.

Expected Codex reasoning/model:

```text
Codex Reasoning: Medium
ChatGPT Model: GPT-5.5
```

Escalate to:

```text
Codex Reasoning: High
ChatGPT Model: GPT-5.5 Thinking
```

only if the annotated trace fails because of a suspected simulator/game-mechanic mismatch.
