# Next Task Preview — HSR-AXIS-002N

Proceed only after HSR-AXIS-002M-FIX passes.

Because the corrected 002M readiness should be `blocked_by_both`, 002N should remain evidence-only and resolve the source and duration-semantics gaps before any executable damage-buff binding.

Likely scope:

- capture a version-qualified release/live Tingyun Ultimate damage-increase level table with exact field locators;
- preserve the real-video Tingyun trace level as unknown;
- determine whether effect order is represented in structured release data or requires a controlled interaction test;
- establish the same-current-turn duration rule from release behavior or a reproducible in-game test;
- normalize the new evidence atomically;
- recompute readiness without changing simulator execution.

Recommended setup:

- Codex Reasoning: **High**
- Recommended model: **GPT-5.6 Sol**
- Fallback: **GPT-5.6 Terra** only for straightforward local normalization after the source evidence is already captured and unambiguous.

Do not implement the damage buff until a later, separately reviewed binding task receives `ready_for_separate_binding_task`.
