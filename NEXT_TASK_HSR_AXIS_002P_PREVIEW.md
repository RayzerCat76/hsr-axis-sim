# NEXT TASK PREVIEW — HSR-AXIS-002P

Do not begin until HSR-AXIS-002O passes independent review.

Likely next gate depends on 002O:

## If generic semantics are fully resolved

Create a **parameterized reviewed partial binding** for Tingyun Ultimate's DMG buff. The caller must provide:

- a legal selected ally;
- an explicit trace level 1–15.

The binding must derive magnitude from the validated table rather than hard-code Lv. 10. It must remain separate from accepted-video replay binding while the video's target and trace level are unknown.

## If generic semantics remain unresolved

Create the smallest evidence-acquisition or configurable-policy task needed to resolve the remaining semantic edge. Do not implement AddBuff under an assumed policy.

Suggested configuration:

- Codex Reasoning: High
- Model: GPT-5.6 Sol
- Terra only after all semantics are explicit and the work becomes routine implementation.
