Task ID: HSR-AXIS-001C
Reasoning: HIGH

You are continuing the same permanent Python project for a Honkai: Star Rail inspired action-axis simulator.

Previous task status:
- HSR-AXIS-001A implemented the deterministic action-value simulator core.
- HSR-AXIS-001A-FIX added safety checks and stricter tests.
- HSR-AXIS-001B added the MVP Golden Replay Validator.
- Lumen locally ran the uploaded 001B test suite and got: 34 passed.

Goal of this task:
Harden the replay validator and add a multi-step Bronya-like + Seele-like golden replay that actually exercises action-axis reproduction across several steps.

Important scope limits:
- Do NOT scrape Huroka, Yatta, HoneyHunter, Bilibili, or any external website.
- Do NOT implement real HSR character kits.
- Do NOT implement the full damage formula.
- Do NOT implement enemy AI.
- Do NOT implement beam search or AI axis search yet.
- Preserve all existing 001A/001B behavior and tests.

Core idea:
The current 001B golden replay only has one step. 001C should prove the validator can reproduce a simple multi-step axis:

1. Seele-like DPS acts first.
2. Bronya-like support acts next.
3. Bronya-like support uses an action that immediately advances Seele-like DPS.
4. Seele-like DPS acts again at the same global AV point because her current_av was set to 0.

This is still a simplified generic simulation. Do not encode real Bronya or Seele kits. Use generic `Action` plus effect primitives.

Required new/changed files:

hsr_axis_sim/
  sim/
    replay.py
  data/
    golden_replays/
      bronya_seele_multistep_mvp.json
  tests/
    test_replay_validator.py
  LUMEN_RESULT.md

Required replay file:
Create `hsr_axis_sim/data/golden_replays/bronya_seele_multistep_mvp.json`.

Suggested initial state:

- `global_av`: 0
- `skill_points`: 3
- `max_skill_points`: 5
- units:
  - `seele`
    - base_speed: 143
    - speed: 143
    - current_av: 69.9300699301
    - energy: 60
    - max_energy: 120
    - hp: 3000
    - max_hp: 3000
  - `bronya`
    - base_speed: 134
    - speed: 134
    - current_av: 74.6268656716
    - energy: 80
    - max_energy: 120
    - hp: 3200
    - max_hp: 3200
  - `enemy_1`
    - base_speed: 100
    - speed: 100
    - current_av: 100
    - hp: 20000
    - max_hp: 20000

Suggested actions:

1. `seele_basic`
   - actor_id: `seele`
   - target_ids: [`enemy_1`]
   - effects:
     - `GainSkillPoint(amount=1)`
     - `GainEnergy(amount=20, target_ids=["seele"])`
     - `DealDamage(amount=1000, target_ids=["enemy_1"])`
   - ends_turn: true

2. `bronya_skill_like`
   - actor_id: `bronya`
   - target_ids: [`seele`]
   - effects:
     - `ConsumeSkillPoint(amount=1)`
     - `GainEnergy(amount=30, target_ids=["bronya"])`
     - `ImmediateAction(target_ids=["seele"])`
   - ends_turn: true

3. `seele_skill_like`
   - actor_id: `seele`
   - target_ids: [`enemy_1`]
   - effects:
     - `ConsumeSkillPoint(amount=1)`
     - `GainEnergy(amount=30, target_ids=["seele"])`
     - `DealDamage(amount=2500, target_ids=["enemy_1"])`
   - ends_turn: true

Expected hand-audited timeline:

Step 1:
- selected actor: `seele`
- elapsed AV: 69.9300699301
- global_av: 69.9300699301
- after action and turn end:
  - skill_points: 4
  - seele.current_av: 69.9300699301
  - seele.energy: 80
  - bronya.current_av: approximately 4.6967957415
  - enemy_1.current_av: approximately 30.0699300699
  - enemy_1.hp: 19000

Step 2:
- selected actor: `bronya`
- elapsed AV: approximately 4.6967957415
- global_av: approximately 74.6268656716
- after action and turn end:
  - skill_points: 3
  - bronya.current_av: 74.6268656716
  - bronya.energy: 110
  - seele.current_av: 0
  - enemy_1.current_av: approximately 25.3731343284

Step 3:
- selected actor: `seele`
- elapsed AV: 0
- global_av remains approximately 74.6268656716
- after action and turn end:
  - skill_points: 2
  - seele.current_av: 69.9300699301
  - seele.energy: 110
  - bronya.current_av: 74.6268656716
  - enemy_1.current_av: approximately 25.3731343284
  - enemy_1.hp: 16500

Use tolerance 0.001 in the JSON replay.

Replay validator hardening requirements:

1. Add duplicate unit ID validation.
   - If `initial_state.units` contains duplicate `id` values, validation should fail clearly.
   - Prefer returning a failed `ReplayCheckResult` rather than throwing an uncaught exception from `validate()`.

2. Add explicit test coverage for step-level target override.
   - Create a replay/action setup where the action default target differs from the step `target_ids`.
   - Assert that the step target override determines the actual affected unit.

3. Add explicit test coverage for multi-step replay.
   - Load `bronya_seele_multistep_mvp.json`.
   - Assert `passed is True`.
   - Assert `checked_steps == 3`.

4. Add explicit test coverage that the validator reports useful mismatch messages from the multi-step replay.
   - Example: mutate step 2 expected `seele.current_av` from 0 to 10 and assert the mismatch includes `step 2` and `seele.current_av`.

5. Accept but ignore `forced_rng` for now.
   - If a replay step contains a `forced_rng` dictionary, the validator should not fail merely because the field exists.
   - Do not implement real RNG behavior yet.
   - Add a test proving `forced_rng` can appear in a step without causing a mismatch.

6. Add a command-line runner for replay validation.
   - Running this should validate a replay file and print a readable report:
     `python -m hsr_axis_sim.sim.replay hsr_axis_sim/data/golden_replays/bronya_seele_multistep_mvp.json`
   - On pass, print a concise success message including replay name and checked step count.
   - On fail, print mismatches line by line.
   - Return process exit code 0 on pass and nonzero on fail.

7. README update.
   - Add a section showing how to run the multi-step replay from Python and from the CLI.

Testing requirements:
- Preserve all existing tests.
- Add new pytest tests for the requirements above.
- Run the full test suite.

Deliverables:
1. Updated `hsr_axis_sim/sim/replay.py`.
2. New `hsr_axis_sim/data/golden_replays/bronya_seele_multistep_mvp.json`.
3. Updated `hsr_axis_sim/tests/test_replay_validator.py`.
4. Updated `README.md`.
5. Updated `hsr_axis_sim/LUMEN_RESULT.md` with:
   - files changed
   - exact tests run
   - exact pytest result
   - confirmation that the multi-step replay passes
   - known assumptions
   - whether 001D is safe to begin

Do not proceed to Huroka import, real damage formula, real character kits, or AI search in this task.
