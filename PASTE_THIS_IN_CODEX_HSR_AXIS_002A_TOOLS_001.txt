Task ID: HSR-AXIS-002A-TOOLS-001
Codex Reasoning: Medium
ChatGPT Model recommendation: GPT-5.5

Reason for this combination:
This task is mostly CLI, file generation, fixture writing, and tests. Do not modify combat core mechanics. GPT-5.5 should be sufficient; GPT-5.5 Thinking is only needed later if video-derived traces fail and we must diagnose simulator-vs-game mismatches.

You are working in the existing HSR Axis Simulator project.

Goal:
Build a small offline Video Trace Assistant MVP that helps us convert a local gameplay video into a human-annotatable manual trace intake package.

Important:
Do NOT download from Bilibili.
Do NOT scrape Bilibili.
Do NOT add OCR yet.
Do NOT auto-classify characters or skills.
Do NOT modify simulator core mechanics.
Do NOT add this draft to the locked regression manifest.
Do NOT pretend the draft trace is replay-ready.

Context for the first real trace candidate:
- Game: Honkai: Star Rail
- Scenario: 3.4 博徒困境 第12层 第一面
- Bilibili URL: https://www.bilibili.com/video/BV1CXtVzaEQB
- Video title: 【3.4博徒困境】全网首发！0+1风套那刻夏逆属性2金0t砂金！
- Team: 那刻夏 / 停云 / 佩拉 / 记忆主
- Pre-battle: 佩拉秘技开怪
- Confirmed opening sequence v0.3:
  1. 停云终结技
  2. 佩拉战技
  3. 记忆主战技
  4. 停云战技
  5. 佩拉终结技
  6. 那刻夏终结技
  7. 那刻夏普攻 + 额外战技
  8. 迷迷拉条那刻夏
  9. 那刻夏战技 + 额外战技

Implement:

1. New module:
   hsr_axis_sim/tools/video_trace_assistant.py
   hsr_axis_sim/tools/video_trace_models.py

2. CLI command:

   python -m hsr_axis_sim.tools.video_trace_assistant \
     --video data/video_trace_sources/sample1.mov \
     --trace-name real_video_trace_001_botu_dilemma_3_4_floor12_side1_opening \
     --start 0 \
     --end 16 \
     --interval 0.5 \
     --output-dir data/video_trace_intake/sample1_botu_dilemma_opening \
     --source-platform bilibili \
     --source-url "https://www.bilibili.com/video/BV1CXtVzaEQB" \
     --video-title "【3.4博徒困境】全网首发！0+1风套那刻夏逆属性2金0t砂金！" \
     --uploader "unknown" \
     --notes "Opening sequence only; intake draft; not replay-ready."

3. Generated output structure:

   output-dir/
     frames/
       t_000.0.jpg
       t_000.5.jpg
       ...
     frame_index.csv
     trace_annotation_worksheet.md
     draft_trace.json
     README.md

4. frame_index.csv columns:

   frame_id,timestamp_seconds,timestamp_label,frame_path,annotation_status,notes

5. trace_annotation_worksheet.md must include:
   - source metadata;
   - selected video range;
   - frame index table;
   - known opening sequence v0.3;
   - blank human action table with columns:
     step, video_timestamp, actor, action, target, sp_before, sp_after, energy_before, energy_after, hp_change, toughness_change, forced_rng, notes
   - missing fields checklist.

6. draft_trace.json must include:
   - name;
   - source metadata;
   - status: intake_draft;
   - replay_ready: false;
   - team notes;
   - declared_sequence containing the confirmed v0.3 opening sequence;
   - empty or placeholder step records that clearly require human completion.

7. Video extraction backend:
   Use ffmpeg via subprocess.
   If ffmpeg is missing, raise a clear error message.
   Keep tests independent of real videos by mocking subprocess or testing pure writer functions.
   Do not add heavy dependencies unless already present.

8. Tests:
   Add tests for:
   - timestamp generation;
   - CSV writer;
   - worksheet writer;
   - draft trace JSON fields: status=intake_draft and replay_ready=false;
   - clear ffmpeg-missing error;
   - existing regression manifest is not changed.

9. LUMEN_RESULT.md:
   Include:
   - files changed;
   - tests run;
   - passing/failing tests;
   - how to run this on sample1.mov;
   - what is intentionally not implemented;
   - next recommended step.

Acceptance:
- All existing tests pass.
- New tests pass.
- Tool can generate frames + CSV + worksheet + draft JSON from a local video.
- Draft trace is clearly marked not replay-ready.
- No simulator core mechanics changed.
- No fake trace added to locked manifest.
